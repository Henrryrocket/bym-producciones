---
import { onMount } from "solid-js";

const bgImageUrl = "https://source.unsplash.com/1600x900/?music,studio";
---

<!-- HeroComponent.astro --><!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BYM Producciones</title>
  </head>
  <body>
    <div class="relative h-screen bg-black">
      <img
        src={bgImageUrl}
        alt="Studio background"
        class="absolute inset-0 w-full h-full object-cover opacity-60"
      />
      <div
        class="relative z-10 flex flex-col items-center justify-center h-full px-4 sm:px-6 lg:px-8"
      >
        <h1 class="text-4xl font-bold text-yellow-500 sm:text-6xl lg:text-8xl">
          BYM PRODUCCIONES
        </h1>
        <p class="mt-4 text-xl text-yellow-300 sm:mt-6 lg:text-2xl">
          LLEVAMOS TU MÚSICA AL SIGUIENTE NIVEL
        </p>
      </div>
    </div>
  </body>
</html>
